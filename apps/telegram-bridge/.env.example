## Networking
# Bind the bridge port only on your Tailscale IP (optional)
# Replace with your Umbrel node's Tailscale IP (e.g., 100.64.x.y)
# If unset, Docker binds on all interfaces (0.0.0.0)
# HOST_BIND_IP=100.64.12.34

## Env-driven config (rendered into /data/config.yaml)
# Required (set ALLOW_EMPTY=1 to bypass)
# HOMESERVER_ADDRESS=http://synapse.example.com:8008
# MATRIX_DOMAIN=your.matrix.domain
# APPSERVICE_ADDRESS=http://umbrel.<tailnet>.ts.net:29317

# Tokens (ensure they match registration.yaml)
# HS_TOKEN=change-me-hs
# AS_TOKEN=change-me-as

# Optional
# BOT_USERNAME=telegrambot

# Behavior flags
# RENDER_CONFIG=1        # Force re-render on next start
# ALLOW_EMPTY=0          # Allow empty values if set to 1
# BACKUP_CONFIG=1        # Backup existing /data/config.yaml before rendering
